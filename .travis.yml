language: php
php:
  - 7.1


before_install:
- composer require phpunit/phpunit:~7.0 satooshi/php-coveralls:~2.0
- composer install --dev
 
script:
- mkdir -p build/logs
- php vendor/bin/phpunit -c phpunit.xml.dist
 
after_script:
- php vendor/bin/php-coveralls -v